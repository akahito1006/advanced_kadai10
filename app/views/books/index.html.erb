<div class="col-md-3">
  <%= render 'users/info', user: @user %>
  <%= render 'books/new', book: @book %>
</div>



<% @books.each do |book| %>
<div>
  <p>User: <%= book.user.id %></p>
  <p>Title：<%= book.title %></p>
  <p>Opinion：<%= book.body %></p>
</div>
<% end %>





books#index